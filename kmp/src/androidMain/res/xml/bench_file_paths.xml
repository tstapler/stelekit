<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Serves context.cacheDir so the benchmark FileProvider can expose graph files
         written there during AndroidGraphBenchmark tests. -->
    <cache-path name="bench" path="." />
</paths>

# Pre-mortem: commonmain-bazel-target-split
**Date**: 2026-09-04

## Failure Modes

| # | Failure | First Symptom | Prevention | Severity |
|---|---------|--------------|------------|----------|
| 1 | Epic 1.7's ABI-stability spike (Story 1.7.2) comes back FAIL, but the plan's only written contingency is "root-caused... or explicitly accepted as a known limitation with the go/no-go checkpoint's expectations adjusted accordingly" — not a real fix. Tier 1/2 ship, the Success Metrics' narrow per-package mechanism check still passes (Epic 3.1 only tests `stats` in isolation), but since 87.8% of the codebase (Tier 3) is untouched, every Tier-3-touching PR still forces re-execution (not just re-analysis) of every already-extracted Tier 1/2 `KotlinCompile` action, so the aggregate CI-time win most real PRs care about never materializes. | Weeks after Tier 2 ships, engineers report PRs touching `ui`/`db`/`repository` (Tier 3) feel exactly as slow as before, while the project's own dashboards/checkpoint still show green — there is no automated signal that catches this because Story 3.1.1's PASS/FAIL criteria never measured the aggregate case, only the isolated one. | Make Story 1.7.2's FAIL path a hard stop, not a soft "adjust expectations": if the ABI-stability check fails, require Task 3.1.1e (currently optional, real-PR-touch-pattern sampling) to become mandatory, and add a numeric go/no-go criterion to Epic 3.1 — e.g. "≥X% of the last 50 merged PRs touching `commonMain` would see a measurable win" — computed and recorded, not assumed, before Phase 4 is allowed to start. | P1 |
| 2 | Epic 1.3's `.bazelrc` worker cap addresses only the execution-phase persistent-worker-heap OOM vector (the exact mechanism from commits `f8e7aa82e1`/`6842053f8d`) but not the analysis-phase OOM vector `research/pitfalls.md` itself flags as a second, independent risk (rules_kotlin issue #1043 — larger dependency-graph paths through ~29 new targets increasing transitive-output-info merging cost in Bazel's own analysis phase, not any worker's heap). Task 1.3.1b's `free -h` sampling is only before/after the build step, which — per the adversarial review's own Minor note — can miss a transient mid-build spike exactly like the original incident's transient spike. | A CI Bazel job on a routine, unrelated PR (not even a `commonMain`-touching one) fails with an OOM/terminate-signal error after Tier 2 lands and ~29 targets exist, while the pre/post `free -h` snapshots both look fine — reproducing the "took two separate CI runs to fully characterize" pattern of the original incident, but for a mechanism (analysis-phase graph-size cost) the worker cap never addressed. | Add a dedicated analysis-phase memory check to Phase 1 (extend Epic 1.3 or add a new story): a continuous (not before/after) memory-sampling loop during the actual CI Bazel job, or `bazel build --profile=...` analyzed specifically for the loading/analysis phase, run once with ~2 targets (today) and once with the full ~29-target graph (end of Tier 2) to confirm the analysis phase's own memory footprint doesn't approach the 16GB ceiling. | P2 |
| 3 | Epic 3.1's go/no-go checkpoint only exercises the Tier 1 mechanism — 9 zero-`internal`, zero-cross-tier-associates leaf packages — using `stats` as the sole worked example. It never exercises the harder, `associates`-from-still-monolithic-target-into-a-newly-extracted-package direction (`cache`→`performance`, Story 4.1.1) before greenlighting all of Phase 4. That pattern is first exercised for real only partway *into* Tier 2's rollout, after several Tier 2 PRs may have already merged. | Mid-way through Tier 2 (e.g. at Story 4.1.1, the first production use of monolith→extracted-package `associates`), CI fails or IDE tooling misresolves `internal` visibility in a way Epic 2.1's 2-target smoke test never modeled, forcing a scramble to fix or roll back several already-merged Tier 2 PRs rather than stopping cleanly at a planned checkpoint. | Add an explicit stop-and-verify point after Story 4.1.1 specifically (not just at the start of Phase 4): confirm the monolith→`:cache` `associates` wiring is clean in real CI (not just the Epic 2.1 smoke test) before Stories 4.1.3–4.1.11 proceed, so the riskiest new mechanism is validated on its own before spending the rest of Tier 2's PR budget. | P2 |
| 4 | Across ~20 near-identical per-package extractions (Epic 2.2 ×9, Epic 4.1 ×11), the repeated 3-task recipe invites shortcut-taking on later packages — not just a skipped grep (which fails fast at compile time) but a silent, unrecorded decision to make a symbol `public` instead of wiring `associates`, exactly as Task 4.2.1b already explicitly sanctions for `util.roundTo` ("a single trivial extension function... no `associates` entry is needed"). Nothing in the plan flags a `internal`→`public` change as needing scrutiny; no lint or CI check watches for encapsulation erosion across the 20-package rollout. | No CI failure at all — the build stays green. Months later, an architecture review or a new engineer notices several formerly-`internal` symbols across the migrated packages are now `public` with no comment explaining why, and the "internal boundary" tier_manifest.bzl was supposed to preserve has quietly eroded package-by-package. | Extend `check_internal_visibility.py` (Epic 1.4) or add a small CI diff-check that flags any `internal`→`public` visibility change on a PR touching `kmp/src/commonMain/kotlin/`, requiring an explicit one-line justification in the PR description (mirroring the plan's own Task 4.2.1b reasoning) rather than letting it pass as an implicit side effect of avoiding `associates` wiring. | P2 |
| 5 | The plan defines `tier_manifest.bzl` as a static snapshot of today's 38 packages, and Tier 3's continued existence depends on the `kt_srcs` monolith glob (`glob(["**/*.kt"])` minus excludes) as the catch-all default. Nothing in the plan defines what happens when a *new* top-level package is added to `commonMain` after this project ships — it silently falls into the still-existing monolith glob by default, with no lint comparing `tier_manifest.bzl`'s tracked packages against the actual directory listing. | Six months post-ship, a new package (e.g. a hypothetical `annotations/`) is never extracted into its own Bazel target; nobody notices because no check compares `tier_manifest.bzl` ∪ the monolith's remaining scope against `ls kmp/src/commonMain/kotlin/dev/stapler/stelekit/`. New contributors have no documented answer to "where does my new package's BUILD.bazel go," and the repo permanently carries two undocumented BUILD conventions side by side. | Add a CI check (extend Epic 1.8's duplicate-ownership script or add a sibling one) asserting every top-level directory under `kmp/src/commonMain/kotlin/dev/stapler/stelekit/` is accounted for by exactly one of {a `tier_manifest.bzl` list, the monolith's remaining glob scope} — failing loudly (forcing an explicit classification decision, even if that decision is "added to Tier 3 by default") rather than silently defaulting. Add a one-paragraph "new package onboarding" note recording that decision process. | P2 |

## P1 Items (address before implementation)
- [x] Failure #1 — **Resolved in plan.md** (Phase 4 repair pass, 2026-09-04): Story 1.7.2 now defines an explicit FAIL-path decision gate (branch (a) re-scope-and-continue vs. branch (b) hard-stop-and-redesign), Task 1.7.2c requires recording which branch was chosen, and Story 3.1.1 gained a numeric go/no-go criterion (≥50% of the last 50 merged commonMain-touching PRs must touch only Tier 1/2) that becomes mandatory — not optional — when branch (a) is chosen. The two checkpoints are cross-referenced as the same underlying risk caught twice.

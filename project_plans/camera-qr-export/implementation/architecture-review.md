# Architecture Review: camera-qr-export
**Date**: 2026-07-11
**Verdict**: CLEAN

Grounding note: this is a re-verification pass (iteration-2 re-check) against the actual current files, not the fix subagent's summary. Checked directly: `project_plans/camera-qr-export/implementation/plan.md` (full 708 lines), `project_plans/camera-qr-export/requirements.md` (full), and `kmp/src/commonMain/kotlin/dev/stapler/stelekit/ui/annotate/AnnotationEditorViewModel.kt` (lines 95-134, confirmed `DepthModelUiState` sealed interface at line 115). Grepped plan.md for every "QrScanner.kt" mention, every Task-2.1.x / Task-3.2.2.x ID, and every "does not call/touch" phrase to check for stale contradictory language.

Of the prior re-check's 1 blocker + 1 nitpick, both are now resolved, and no new issues were introduced by this second fix pass.

---

## Blockers
None.

## Concerns
None.

## Nitpicks
None.

## Verified Resolved
- **Blocker — seam-purity vs. `ScanHint` contradiction (Contradiction A)**: Story 2.1.1's AC (plan.md:313-314) only constrains the `FrameTransport` interface's own signature ("no parameter or return type references `QrMatrix`, `CameraFrame`, or any QR type") and makes no claim at all about `QrTransferCoordinator`. Story 3.2.2 (plan.md:522-523) now gives `QrTransferCoordinator` **two constructor collaborators**: `frameTransportReceiver: FrameTransportReceiver` (the sole transfer-data path — frames → `ChunkFrameCodec.decode` → `TransferSession`/`ChunkBuffer` → `reassemble()`) and a directly-injected `qrScanner: QrScanner` (diagnostics-only, deriving `ScanHint` from `ScanResult`/luminance; its output "never feeds `ChunkBuffer`," stated explicitly at plan.md:523, 539, 547, 75). The "one documented exception" rationale is stated inline, not merely implied: "rich scan diagnostics are a legitimately QR-specific UX feature (gap G5) that the generic, medium-neutral `FrameTransport` seam is not meant to serve — inventing a generic `FrameEvent`/`SignalQuality` taxonomy on the seam just to carry this one QR-specific feature would over-engineer the seam for a need no other transport (WebRTC/Bluetooth/audio) actually has" (plan.md:523). Grepped for lingering "does not call QrScanner directly" language against the coordinator specifically — the only remaining "does not touch" phrasing (plan.md:522) correctly scopes to `QrDecodeViewModel`, not the coordinator, which is coherent since the ViewModel delegates the whole pipeline to the coordinator and touches neither `FrameTransportReceiver` nor `QrScanner` itself. No contradiction remains between the two stories.
- **Blocker — `QrScanner.kt` double file ownership (Contradiction B)**: grepped all 7 "QrScanner.kt" mentions in plan.md. Only one — Story 2.1.4's Files line (plan.md:370) and Task 2.1.4b (plan.md:376-378, "Create `QrScanner.kt`") — marks it `(new)`. Every other mention is explicit about non-recreation: Domain Glossary `QrFrameTransport` row (plan.md:54, "Story 2.1.4, which also creates `QrScanner.kt`"), Domain Glossary `QrScanner` row (plan.md:55, "single file, single creation point (Story 2.1.4) — Story 3.2.2 references it, does not recreate it"), Story 3.2.2's Files line (plan.md:532, "reference existing — created in Story 2.1.4; this story adds its second, diagnostics-only consumer, it does not recreate the file"), Task 3.2.2a (plan.md:535, "this task does not create or modify `QrScanner.kt`"), Task 3.2.2b (plan.md:539, "existing, from Story 2.1.4"). Single creation point confirmed structurally, not just asserted once.
- **Nitpick — Story 2.1.4 forward-reference to `CameraFrameSource`**: a one-line prerequisite note was added (plan.md:364): "this story's receive-side composition also depends on `CameraFrameSource.frameStream()` — see Story 2.2.1 — even though Story 2.2.1 follows this one in reading order; both land in the same Phase 2 delivery unit, per the Dependency Visualization." Resolves the top-to-bottom reader confusion flagged previously.
- **Minor — `DepthModelUiState` citation correctness**: spot-checked directly against `kmp/src/commonMain/kotlin/dev/stapler/stelekit/ui/annotate/AnnotationEditorViewModel.kt:115` — it is genuinely `sealed interface DepthModelUiState { Absent, Downloading(progress: Int), Ready, Failed }`, with KDoc at line 110 stating it is a "Platform-independent mirror of `DepthModelDownloader.ModelState` for common UI," populated from the Android platform layer (line 112-113, confirmed also in `DepthEstimationCoordinator.kt` and `OnnxMonocularDepthEstimator.kt`). Plan.md's citations (line 76, line 93) accurately describe this as the correct `commonMain` analog for `QrEncodeUiState`/`QrDecodeUiState`, correctly reverted from the earlier incorrect `DepthModelDownloader.ModelState` (androidMain) citation.
- **requirements.md reconciliation**: Success Metrics (requirements.md:19) now carries the parenthetical: "(v1 implementation scope, per plan.md, cuts this to single-page transfer only — see plan.md's 'v1 scope — explicit cuts' section. Multi-page/graph-set transfer is deferred to a follow-on; the single-page flow run once per page satisfies this gate for v1.)" — correctly cross-references plan.md's scope cut.
- **Fresh check — no new contradiction from the dual-injection design**: the `qrScanner` diagnostics collaborator does not disturb any round-1 fix. `TransferSession`/`ChunkBuffer`/`VerifiedTransferPayload` still have exactly one data source (`frameTransportReceiver.frames()`); the `qrScanner`'s `ScanResult`/luminance output is explicitly and repeatedly stated to never reach `ChunkBuffer` (plan.md:75, 523, 539, 547), so "only `reassemble()`'s success branch mints a `VerifiedTransferPayload`" (the round-1 Parse-Don't-Validate fix) remains intact.
- **Fresh check — Task ID renumbering**: `Task 2.1.4a` / `2.1.4b` (QrScanner.kt creation) / `2.1.4c` are sequential with no gap or duplicate; `Task 3.2.2a` / `3.2.2b` / `3.2.2c` / `3.2.2d` likewise sequential. No adjacent-task ID collisions introduced by moving the `QrScanner.kt` creation task into Epic 2.1.

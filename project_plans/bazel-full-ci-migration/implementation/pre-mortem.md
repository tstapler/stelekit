# Pre-mortem: bazel-full-ci-migration

**Date**: 2026-06-24

## Failure Modes

| # | Failure | First Symptom | Prevention | Severity |
|---|---------|--------------|------------|----------|
| 1 | Custom Detekt rules silently not loading — `SteleKitRuleSetProvider` META-INF/services file absent from JAR; `bazel test //kmp:detekt` passes green but enforces none of the 15 custom rules | A contributor introduces a `directDatabaseWrite` call without `@OptIn`; Detekt green; violation ships to main | Make `resources = glob(["src/main/resources/**"])` non-negotiable in `buildSrc/BUILD.bazel` Task 1.2.1a snippet; run MINOR-5's smoke-check (jar -tf + grep services) as a required CI gate before removing `continue-on-error` | P1 |
| 2 | `detekt-api` version skew (`1.23.7` custom rules JAR loaded by `1.23.8` SPI) causes `AbstractMethodError` or silent `ClassCastException`; Detekt silently degrades to core-rules-only | First run of `bazel test //kmp:detekt` on a clean CI machine; no actionable error message — Detekt exits 0, no violations | Pre-flight: build `//buildSrc:detekt_rules` locally against the `rules_detekt 0.8.1.13` classpath before merging Epic 1.1; document outcome in PR description (CONCERN-5 resolution) | P1 |
| 3 | `verify_android_screenshots` silently passes because `gradlew` is not found — `BUILD_WORKSPACE_DIRECTORY` is empty in `bazel test` context and fallback walk-up logic hits `/` without finding `gradlew` (e.g., CI runner home differs from local machine), causing `exit 1` to be swallowed by `sh_test` misconfiguration | `bazel test //kmp:verify_android_screenshots` exits 0 on first CI run; screenshot regressions go undetected | Validate the fallback path in `verify_android_screenshots.sh` on a clean GHA runner before enabling the CI gate; add an explicit `echo "WORKSPACE=$WORKSPACE"` debug line to the script so workspace detection is visible in CI logs | P2 |
| 4 | `bazel-roborazzi` job always fails because `GRADLE_ENCRYPTION_KEY` secret is absent in `bazel-ci.yml` (secret exists only in `ci.yml`); `gradle/actions/setup-gradle` fails cache restore; every main-branch push emits a failing job | First push to main after Epic 2.2 merges; `bazel-roborazzi` job fails on cache restore step; `continue-on-error: true` masks it as a warning | Before merging Epic 2.2, verify `GRADLE_ENCRYPTION_KEY` exists in repo Secrets (Settings → Secrets → Actions); if missing, add it or remove `cache-encryption-key` from the job | P2 |
| 5 | Story 2.3.1 (Roborazzi graduation) never executes — Android baseline determinism is never confirmed; `verify_android_screenshots` stays `tags = ["manual"]` indefinitely; dual-system (Gradle + Bazel record) runs in parallel for 6 months | Six months after Epic 2.2 merges, the `android` Gradle CI job still runs `recordRoborazziDebug`; no graduation PR is opened; Success Metrics #3 and #5 are never achieved | Set a hard calendar deadline for resolving Unresolved Question #2 (baseline determinism) — not "deferred until confirmed" but "confirmed or descoped by [date]"; add a GitHub issue with due date at Epic 2.2 merge | P2 |
| 6 | `MODULE.bazel.lock` churn from new Maven deps (`detekt-api`, `io.nlopez.compose.rules:detekt`) causes intermittent `bazel mod deps` failures during the migration PR; contributors see lock file conflicts; Epic 1.1 PR is blocked in review for days | First CI run of the Epic 1.1 PR shows a lockfile drift error or pin conflict; `@maven//:pin` re-generates a diff that conflicts with a concurrent dependency PR | Isolate Epic 1.1 as a standalone PR with no concurrent dep changes; run `bazel run @maven//:pin` immediately before opening the PR; add the lockfile diff to the PR description so reviewers understand the scope | P3 |
| 7 | `bazel-detekt` CI job fails on toolchain resolution, not Detekt logic — `actions/setup-java` step missing (BLOCKER-1 from adversarial review); intermittent failures on GHA image upgrades | `bazel-detekt` job fails at `bazel test //kmp:detekt` with "no suitable Java toolchain found"; appears to be a flaky environment issue, not a code issue | Add `actions/setup-java@v4` (java-version: "21", distribution: "temurin") as a required step in `bazel-detekt` before the Bazel step — the adversarial review already flagged this as BLOCKER-1; it must be in Task 1.4.1a before the PR merges | P1 |
| 8 | Staged rollout `continue-on-error: true` is never removed — nobody tracks the "3 consecutive green runs" graduation criterion; Detekt runs in Bazel and Gradle in parallel indefinitely; a Bazel Detekt failure is invisible to contributors | Six weeks after Epic 1.4 merges, `bazel-detekt` shows a flake in CI logs; nobody acts because `continue-on-error` suppresses it; Gradle lint remains the only real gate | Define the graduation criterion mechanically: add a TODO comment with a link to a tracking GitHub issue in the `bazel-detekt` job, and verify the issue is closed before beginning Epic 1.5 work | P2 |

## P1 Items (address before implementation)

- [ ] **Failure #1** — Make `resources = glob(["src/main/resources/**"])` a mandatory (non-conditional) line in the `buildSrc/BUILD.bazel` `kt_jvm_library` snippet in Task 1.2.1a; verify the services file is present in the built JAR (`jar -tf bazel-bin/buildSrc/detekt_rules.jar | grep services`) as a required check before the Epic 1.3 PR can merge
- [ ] **Failure #2** — Pre-flight Task 1.1.1pre must be completed and documented in the Epic 1.1 PR description before any MODULE.bazel changes are merged; if `detekt-api:1.23.7` is incompatible, switch to `detekt-api:1.23.8` in the Maven artifact list
- [ ] **Failure #7** — Add `actions/setup-java@v4` (java-version: "21", distribution: "temurin") to the `bazel-detekt` job steps in Task 1.4.1a, before the `Set up Bazel` step, matching the pattern in all other `bazel-ci.yml` jobs
